type Query {
  jobs: [Job!]
}

type Job {
  id: ID!
  title: String!
  """
  it's not a normal string but it's a 
  ISO kind date of when the comment is 
  posted
  """
  date: String!
  description: String
}
